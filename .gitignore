# Hugo
/public/
/resources/
/.hugo_build.lock

# Sass
.sass-cache/
*.css.map

# Editors
.vscode/
.idea/
.claude/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Research/discovery outputs (not for version control)
bill-discovery.txt

# Python
__pycache__/
*.pyc
*.pyo

# Pipeline caches (BILLSTATUS bulk ZIPs, etc.)
scripts/.cache/
# …except the legislators lookup, which is committed so the network
# builds work without a one-off fetch step on every machine.
!scripts/.cache/legislators.json

# Node (if we add tooling later)
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.env
.env.local
